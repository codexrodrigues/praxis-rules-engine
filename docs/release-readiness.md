# Release readiness

`0.0.1-SNAPSHOT` e uma linha de desenvolvimento, ainda nao um canal publico. Nao promover silenciosamente.

Pronto localmente: Java 21/Maven 3.9 impostos pelo build; JAR, sources e Javadocs; runtime sem Spring; testes unitarios, conformidade, limites, determinismo, thread safety e consumo neutro.

O repositorio Git canonico e o owner foram definidos como `codexrodrigues/praxis-rules-engine`, com CI de verificacao para `main` e pull requests. Bloqueios externos restantes: license, developers, workflow oficial por tag, assinatura, Central Portal, coordenada publica e estrategia de versao ainda precisam de aprovacao. Nenhuma tag, deploy ou publicacao deve ser criada antes de resolver esses itens.
