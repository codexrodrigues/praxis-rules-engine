package org.praxisplatform.rules.jsonlogic.internal;

/** Internal sentinel. Never expose it through a public result. */
public enum MissingValue {
    /** Sole identity-based marker for an absent value. */
    INSTANCE
}
